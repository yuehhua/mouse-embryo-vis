# Python tooling (offline mesh-conversion build step) — lives on disk, never pushed
.venv/
.cache/
__pycache__/
*.pyc

# Raw source data + all offline build scratch (deployed copies live in docs/data/)
data/
node_modules/

# OS / editor cruft
.DS_Store
